spring:
  application:
    name: ops-alert-assistant
  profiles:
    default: local
  jackson:
    default-property-inclusion: non_null
  ai:
    openai:
      #
      # Spring AI 的 OpenAI 模型实现同样可以连接 DeepSeek、百炼等
      # OpenAI 兼容接口。这里的占位值只负责让“纯 Mock 开发模式”能够
      # 正常启动；真正调用远程模型前，业务配置层还会检查是否设置了真实 Key。
      # 不要把真实密钥写进配置文件，应通过 AI_DEEPSEEK_API_KEY 环境变量传入。
      api-key: ${AI_DEEPSEEK_API_KEY:not-configured}
      base-url: ${AI_DEEPSEEK_BASE_URL:https://api.deepseek.com}
      chat:
        options:
          model: ${AI_DEEPSEEK_MODEL:deepseek-chat}
          temperature: ${AI_DEEPSEEK_TEMPERATURE:0.2}

server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      probes:
        enabled: true
  metrics:
    tags:
      application: ${spring.application.name}

logging:
  pattern:
    level: "%5p [traceId:%X{traceId:-}]"
  level:
    com.enterprise.opsassistant: INFO

# AI Provider 容错的统一默认策略。代码会为每个 Provider 动态创建独立实例，
# 所以主模型的故障统计、限流配额和熔断状态不会污染备用模型。
resilience4j:
  retry:
    configs:
      default:
        max-attempts: 3
        wait-duration: 300ms
        enable-exponential-backoff: true
        exponential-backoff-multiplier: 2
        ignore-exceptions:
          - io.github.resilience4j.circuitbreaker.CallNotPermittedException
          - io.github.resilience4j.ratelimiter.RequestNotPermitted
  ratelimiter:
    configs:
      default:
        limit-for-period: 20
        limit-refresh-period: 1s
        timeout-duration: 0
        register-health-indicator: true
  circuitbreaker:
    configs:
      default:
        sliding-window-type: COUNT_BASED
        sliding-window-size: 10
        minimum-number-of-calls: 5
        failure-rate-threshold: 50
        wait-duration-in-open-state: 30s
        permitted-number-of-calls-in-half-open-state: 2
        automatic-transition-from-open-to-half-open-enabled: true
        register-health-indicator: true

ops-assistant:
  ai:
    primary-provider: ${AI_PRIMARY_PROVIDER:mock-primary}
    backup-provider: ${AI_BACKUP_PROVIDER:mock-backup}
    max-history-messages: 20
    providers:
      mock-primary:
        enabled: true
        model: mock-ops-primary
        temperature: 0.2
        max-tokens: 2000
        timeout: 30s
      mock-backup:
        enabled: true
        model: mock-ops-backup
        temperature: 0.2
        max-tokens: 2000
        timeout: 30s
